<turbo-frame id="question_<%= question.id %>">
  <div class="card mb-3">
    <div class="card-body">
      <h3 class="card-title"><%= question.content %></h3>
      <!-- Voting form and results as in the main template -->
    </div>
  </div>
</turbo-frame>
